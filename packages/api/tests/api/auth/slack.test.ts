// import express from 'express';
// import { slack } from '../../../src/api/auth/slack';

// const app = express();
// app.use('/slack', slack);

describe('auth SLACK', () => {
  it('redirects to correct url for happy path', async () => {
    // const res: Response = await app.get('/slack');
    // expect(res.redirected).toBe(true);
  });

  it('redirects to incorrect url for unhappy path', async () => {});
});
